[project]
name = "tinyrag"
version = "0.1.0"
description = "Tiny RAG with Zvec"
requires-python = ">=3.10"
dependencies = [
    "zvec>=0.2.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
